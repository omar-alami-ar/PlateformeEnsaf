@model PlateformeEnsaf.Models.ApplicationUser
@{
}

<h1>Index</h1>


<div class="row">
    <div class="col-md-4">
        <form asp-action="Report">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label"> hia</label>
                <select asp-for="Id" class="form-control" asp-items="@(new SelectList(ViewBag.tests,nameof(Model.Id),nameof(Model.UserName)))"></select>
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div>
                <input type="text" class="form-Control" name="body" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

